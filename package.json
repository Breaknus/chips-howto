{
  "name": "chips-howto-guide",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-site.mjs",
    "check": "node tools/build-site.mjs --check && python3 tools/check-links.py"
  },
  "devDependencies": {
    "marked": "17.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
